/** @type {import('next').NextConfig} */
const nextConfig = {basePath: "/my-portfolio", output: "export",images: { unoptimized: true } };
const nextConfig_dev = {basePath: "/my-portfolio",};
export default nextConfig;
